{
  "buildCommand": "cd electricity-demand/frontend && npm install && npm run build",
  "outputDirectory": "electricity-demand/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
